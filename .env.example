# Copier vers .env.local pour les tests locaux, ou définir sur Vercel (Settings → Env).
# URL du backend Flask SANS /api à la fin.
# Prod : voir aussi .env.production (déployé avec le repo).

VITE_API_BASE_URL=https://web-production-f5669.up.railway.app
